const common = {
  main: {
    padding: 12,
    marginTop: 56,
    marginBottom: 56
  }
};

const footer = {
  paper: {
    position: 'fixed',
    bottom: 0,
    width: '100%'
  }
};

const loader = {
  progress: {
    height: window.innerHeight,
    width: window.innerWidth,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
  }
};

export { common, footer, loader };
