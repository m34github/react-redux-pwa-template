import React from 'react';
import { AppBar } from 'material-ui';

const Header = () => (
  <section>
    <AppBar
      title="SimpleApp"
      showMenuIconButton={false}
    />
  </section>
);

export default Header;
