import React from 'react';
import { AppBar } from 'material-ui';

const Header = () => (
  <section>
    <AppBar
      showMenuIconButton={false}
      title="PWA Template"
    />
  </section>
);

export default Header;
