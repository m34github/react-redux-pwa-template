const initialState = {
  payload: {},
  meta: {},
  error: null
};

const home = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default home;
